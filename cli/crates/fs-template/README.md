# fs-template

Tera template engine wrapper: `render_str()` and `from_dir()` for file-based templates.

## Usage

```rust
use fs_template::{render_str, from_dir};
// See src/lib.rs for full API details.
```
